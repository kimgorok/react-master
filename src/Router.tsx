import { BrowserRouter, Route, Routes } from "react-router-dom";

interface IRouterProps {}

function Router() {
  return (
    <BrowserRouter>
      <Routes></Routes>
    </BrowserRouter>
  );
}

export default Router;
